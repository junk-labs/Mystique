<Window xmlns:my="clr-namespace:Mystique.Views.PartBlocks.NotifyBlock"  x:Class="Mystique.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
        xmlns:v="clr-namespace:Mystique.Views"
        xmlns:vm="clr-namespace:Mystique.ViewModels"
        Title="Mystique" Height="300" Width="500"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
    </Window.Resources>
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        
        <!-- STATUS BAR AREA -->
        <my:NotifyBlock Grid.Row="2" DataContext="{Binding NotifyBlockViewModel}" />
    </Grid>
</Window>
